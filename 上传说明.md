# GitHub 上传说明（2026-W39）

> 本目录 = **完整仓库**，含 `episodes/2026-W39/` 这一层。
> 在这一层 `git init` 并 push，远端结构就正好。

## 上传步骤

先在本目录打开终端（或用 `cd` 切到本目录），然后：

```bash
git init
git branch -M main
git add -A
git commit -m "用数学看世界：2026-W39"
git remote add origin https://github.com/donms/math-for-real-world.git
git push -u origin main
```

若远端已有内容（历史期次）：

```bash
git pull --rebase origin main
git push -u origin main
```

## 本仓库包含

```
episodes/2026-W39/
  paper/          论文.md、结论速览.md
  scripts/        求解代码
  results/        json/csv/txt/md + figs/
  data/来源清单.md
  _logs/踩坑.md
```

## 本仓库**不含**（按设定）

| 项 | 原因 |
|---|---|
| `paper/论文.docx` | Word 版自用，不公开 |
| `00_选题卡.md`、`01_题目.md`、`02_题目分析报告.md` | 选题与出题过程，含内部判断 |
| `publish/发布清单.md` | 含平台发布动作与账号运营信息 |
| `内容/` | 四平台发布稿 |
| IndexTTS 模型 / 虚拟环境 / 配音参考音 | 体积与隐私 |

## 已自动处理

* 脚本内**绝对路径全部净化**：本项目根 → `.`；解释器路径 → `python`；
  带用户名的路径 → `<USER_HOME>`
* 剔除下划线开头的内部诊断脚本
* 排除 `__pycache__`、抓取日志与中间产物
